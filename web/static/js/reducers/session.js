const initialState = {
  currentUser: null,
  socket: null,
  error: null,
}

// eslint-disable-next-line no-unused-vars
export default function reducer(state = initialState, action = {}) {
  return state
}
